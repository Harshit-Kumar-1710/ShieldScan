services:
  - type: web
    name: shieldscan-api
    runtime: docker
    rootDir: backend
    healthCheckPath: /health
    envVars:
      - key: ALLOWED_ORIGINS
        sync: false
      - key: OPENAI_API_KEY
        sync: false
